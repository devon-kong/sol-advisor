interface:
  display_name: "Sol Advisor Orchestration"
  short_description: "Risk-gated solo, delegate, audit, or peer full delivery with evidence-bound review"
  default_prompt: "For root startup, load only skill guidance, then declare the route before other task tools; never batch loading with task discovery. Use $orchestration to declare a SELECTIVE ROUTE before task tools; default to solo and select delegation, audit, or exceptional full only when risk justifies it."
