# Quantity contract
All public quantity entry points accept exactly built-in integers 1..20.
A quote costs three units per item. Valid order result fields and actions stay compatible.
This is an offline scalar fixture with no persistence or external effects.
